{% extends 'base.html.twig' %}

{% block title %}Ajouter des clients au cours{% endblock %}

{% block body %}
    <h1>Ajouter des clients au cours</h1>

    <!-- Formulaire d'ajout de client au cours -->
    <div>
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="btn btn-success">Ajouter</button>
        {{ form_end(form) }}
    </div>
{% endblock %}
